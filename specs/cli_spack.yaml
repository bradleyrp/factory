config:
  cli_extensions:
    spack_tree: lib.spack.spack_tree
    spack_hpc_deploy: lib.spack.spack_hpc_deploy
    spack_hpc_run: lib.spack.spack_hpc_run
    spack: lib.spack.spack_router
    rf: # rockfish shortcut
      function: lib.spack.spack_hpc_shortcut
      kwargs: 
        spec: specs/rockfish.yaml