# This is the standard ("std") environment we install in the factory.
name: std
dependencies:
  - pyyaml
  - numpy
  - ipdb
  - snakemake
  - pip
  - pip: 
    # documentation tools
    - sphinx
    - recommonmark
    - numpydoc
